<Window x:Class="VisionSwitcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:om="clr-namespace:OpenMedia.Platform.Controls.Wpf;assembly=OpenMedia.Platform"
        Title="OpenMedia — Vision Switcher" Height="700" Width="1000"
        Background="#1E1E2E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Program Output -->
        <Border Grid.Row="0" BorderBrush="#313244" BorderThickness="0,0,0,2">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="PROGRAM" Foreground="Red" FontWeight="Bold" HorizontalAlignment="Center"/>
                <om:OpenMediaVideoView x:Name="ProgramView" Grid.Row="1" />
            </Grid>
        </Border>
        
        <!-- Controls -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="BtnCutTo1" Content="CUT to Cam 1" Width="120" Margin="4" Click="BtnCutTo1_Click"/>
            <Button x:Name="BtnCutTo2" Content="CUT to Cam 2" Width="120" Margin="4" Click="BtnCutTo2_Click"/>
            <Button x:Name="BtnAddOverlay" Content="Add Overlay" Width="120" Margin="4" Click="BtnAddOverlay_Click"/>
        </StackPanel>

        <!-- Preview Sources -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <Grid Grid.Column="0" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="CAMERA 1" Foreground="#A6ADC8" HorizontalAlignment="Center"/>
                <om:OpenMediaVideoView x:Name="Cam1View" Grid.Row="1" />
            </Grid>
            
            <Grid Grid.Column="1" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="CAMERA 2 (File)" Foreground="#A6ADC8" HorizontalAlignment="Center"/>
                <om:OpenMediaVideoView x:Name="Cam2View" Grid.Row="1" />
            </Grid>
        </Grid>
    </Grid>
</Window>
