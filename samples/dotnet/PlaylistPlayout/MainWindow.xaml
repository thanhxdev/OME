<Window x:Class="PlaylistPlayout.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:om="clr-namespace:OpenMedia.Platform.Controls.Wpf;assembly=OpenMedia.Platform"
        Title="OpenMedia — Playlist Playout" Height="600" Width="1000"
        Background="#1E1E2E" AllowDrop="True" Drop="Window_Drop">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <om:OpenMediaVideoView x:Name="VideoView" />
            
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="▶ Play" Width="80" Margin="4" Click="BtnPlay_Click"/>
                <Button Content="⏸ Pause" Width="80" Margin="4" Click="BtnPause_Click"/>
                <Button Content="⏹ Stop" Width="80" Margin="4" Click="BtnStop_Click"/>
                <Button Content="⏭ Next" Width="80" Margin="4" Click="BtnNext_Click"/>
            </StackPanel>
        </Grid>

        <Border Grid.Column="1" Background="#181825" BorderBrush="#313244" BorderThickness="1,0,0,0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Playlist Items (Drag &amp; Drop)" Foreground="#CDD6F4" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                
                <ListBox x:Name="LstPlaylist" Grid.Row="1" Background="#11111B" Foreground="#CDD6F4" BorderBrush="#313244"/>
                
                <StackPanel Grid.Row="2" Margin="0,10,0,0">
                    <TextBlock x:Name="TxtStatus" Text="Ready." Foreground="#A6ADC8" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
