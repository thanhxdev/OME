<Window x:Class="WpfDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="OpenMedia SDK Demo" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Grid.Row="0"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,20">
            <Button x:Name="BtnStart" Content="Start Engine" Width="120" Height="40" Margin="0,0,10,0" Click="BtnStart_Click"/>
            <Button x:Name="BtnStop" Content="Stop Engine" Width="120" Height="40" Margin="0,0,20,0" Click="BtnStop_Click" IsEnabled="False"/>
            
            <RadioButton x:Name="RbNvenc" Content="Use NVENC" VerticalAlignment="Center" Margin="0,0,10,0" IsChecked="True"/>
            <RadioButton x:Name="RbQsv" Content="Use QuickSync" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Margin="0,0,0,10" Background="Black">
            <local:D3D11VideoPlayer x:Name="VideoPlayer" />
        </Border>

        <TextBox x:Name="TxtLog" Grid.Row="3" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Background="#F0F0F0" FontFamily="Consolas"/>
    </Grid>
</Window>
